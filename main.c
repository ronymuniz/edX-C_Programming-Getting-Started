#include <stdio.h> // Declarações globais: bibliotecas usadas pelo programa

int main(void) { // Início do programa
  printf("Hello World\n"); // Escreve uma mensagem na tela de saída
  return 0; // Retorna o código de erro ZERO para o S.O informando que está tudo OK.
} // Fim do programa.